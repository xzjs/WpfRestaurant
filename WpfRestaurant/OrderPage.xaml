<Page x:Class="WpfRestaurant.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfRestaurant"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="OrderPage">

    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom">
                <Button Width="280" Height="40" Margin="0,10" Background="#FF169BD5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock>总计：￥</TextBlock>
                        <TextBlock>40</TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock FontSize="18" Margin="25,0">大厅</TextBlock>
                    <TextBlock x:Name="tableNoTextblock" FontSize="18" Margin="25,0">A002</TextBlock>
                </StackPanel>
                <StackPanel>
                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="序号" Width="*"></DataGridTextColumn>
                            <DataGridTextColumn Header="菜品" Width="*"></DataGridTextColumn>
                            <DataGridTextColumn Header="数量" Width="*"></DataGridTextColumn>
                            <DataGridTextColumn Header="价格(元)" Width="*"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </StackPanel>
            
        </DockPanel>
    </Grid>
</Page>
